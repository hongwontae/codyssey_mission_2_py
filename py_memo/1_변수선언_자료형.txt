1. 파이썬 변수 선언
    => age = 10 / money = -20000
    => height = 177.8
    => str = "Hong" / str2 = 'Hong'
    => bol = True 

2. 파이썬은 자동으로 자료형을 결정합니다.

3. 파이썬 자료형
    3-1. 정수 (int)
    3-2. 실수 (float)
    3-3. 문자열 (str)
        => 문자열을 출력할 떄 f를 앞에 붙여서 특정 변수와 함께 사용할 수 있습니다.
    3-4. 불리언 (bool)

4. 자료형 확인 함수
    => type(변수)를 사용하면 문자열로 자료형을 알려줍니다.
    => prinft(type(20)) -> <class 'int'>

5. 여러 변수 선언
    => name, age, height = "Tom", 20, 175.5

6. 파이썬 변수 이름 관례
    6-1. 일반 변수, 함수 이름은 snake_case를 사용합니다.
    6-2. 클래스 이름은 CamelCase를 사용합니다.