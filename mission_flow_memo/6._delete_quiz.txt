1. run의 흐름에서 delete_quiz 호출


2. if + not 연산자를 통해 quizzes에 아무것도 없으면 return

3. 존재할 떄 -> show_quiz_list를 통해 보여줌

4. input_number를 통해 삭제할 퀴즈 번호를 받습니다.

5. cofirm을 통해 true/false를 받습니다
    => if not -> False를 가져오면 삭제 취소

6. 삭제 True라면 pop를 통해 삭제합니다.
    => pop은 마지막을 삭제하지만 인자를 받으면 거기에 해당하는 인덱스 요소를 삭제합니다.
    => 이 떄 요소는 0부터 시작 / 삭제할 번호는 1부터 시작 -> -1 처리를 해야합니다.
    => 이 떄 원본 요소를 삭제하기에 반환해서 가지고 있을 필요x

7. 삭제한 뒤 save_state()를 통해 저장