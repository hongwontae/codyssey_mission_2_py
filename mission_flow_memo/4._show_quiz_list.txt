1. show_quiz_list()가 run의 흐름에서 호출됩니다.

2. enumerate + for + self.quizzes를 통해서 저장되어 있는 퀴즈 목록을 돌립니다.
    => 이 떄 편하게 하기 위해서 quiz 객체에서 print_quiz 메서드 구현 
    => 내부에서는 문제 출력 -> 선지를 돌리고 출력하는 작업을 합니다.

3. 마지막으로 정답 출력해야 합니다.
    => quiz 객체에서 quiz.get_answer()를 사용해서 출력합니다.
    => 퀴즈를 파일에서 가져오거나 초기 설정을 사용하거나 퀴즈 추가 모두 정답이 존재
    => answer를 return하고 그걸 출력합니다.